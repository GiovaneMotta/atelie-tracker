{
  "name": "atelie-crm",
  "version": "0.1.0",
  "private": true,
  "description": "CRM Ateliê da Lili — backend (Netlify Functions) + app React. Deps das Functions ficam aqui; o app fica em web/.",
  "engines": { "node": ">=18" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.1",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
